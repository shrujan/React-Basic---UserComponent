import { useSelector } from "react-redux";
import Navbar from "./Navbar";
import CartContainer from "./CartContainer";


const Cart = () => {
  return <main>
    <Navbar />
    <CartContainer />
  </main>
}

export default Cart;
